{
  "scripts": {
    "sync-www": "node scripts/sync-web.js www",
    "sync-web": "node scripts/sync-web.js web",
    "android:sync": "npm run sync-www && npx cap sync android",
    "android:build": "npm run android:sync && cd android && ./gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@tauri-apps/cli": "^2.11.4"
  }
}
